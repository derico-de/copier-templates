import { svelte } from "@sveltejs/vite-plugin-svelte";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [svelte()],
  build: {
    outDir: "../../src/{{ package_folder }}/svelte_apps/static/{{ svelte_app_name }}",
    emptyOutDir: true,
    lib: {
      entry: "src/main.js",
      name: "{{ svelte_app_class }}",
      formats: ["iife"],
      // Fixed names so the bundle registry entries
      // (++plone++{{ package_name }}.svelte/{{ svelte_app_name }}/...) resolve.
      fileName: () => "{{ svelte_app_name }}-bundle.js",
      cssFileName: "{{ svelte_app_name }}-bundle",
    },
  },
});
